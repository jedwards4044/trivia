function sortringArrays() {
    /*
const [extQuestions, setQuestions] = useState<any>({})

//Only run function when questions array is populated
if (Object.keys(extQuestions).length !== 0) {
    sortArrays()
}
function sortArrays() {
    //var sortedQuiz = Object.keys(extQuestions).sort()
    let sortedQuiz: any[] = Object.values(extQuestions)

    //Grab first questions (need to make this loop)
    console.log('question 1', sortedQuiz[0])

    //Create variable for correct and incorrect answers
    let correctAnswer = sortedQuiz[0].correct_answer
    let wrongAnswers = sortedQuiz[0].incorrect_answers

    //create array of all answers
    let allQuestions = []

    //Define right answer is true and push to all Questions array
    let rightAnswer = [{ answerText: correctAnswer }, { isCorrect: true }]
    allQuestions.push(rightAnswer)

    for (let i = 0; i < wrongAnswers.length; i++) {
        //let newWrongs = [{ answerText: wrongAnswers[i] }, { isCorrect: false }]

        allQuestions.push([{ answerText: wrongAnswers[i] }, { isCorrect: false }])
    }

    //Need to shuffle these to be random
    console.log('all', allQuestions)
*/
}

export default sortringArrays
